# Goldseed
## Developing
### Setup
`npm install` or `pnpm install` or `yarn`
### Running
```bash
npm run dev
```

## Building
```bash
npm run build
```
You can preview the production build with `npm run preview`.
